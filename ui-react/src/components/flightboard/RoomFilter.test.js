import RoomFilter from './RoomFilter';
import fetch from 'isomorphic-fetch';

describe('Flightboard RoomFilter', () => {
  // it('renders correctly', () => {
  //   // const spy = jest.
  //   const wrapper = shallow(<RoomFilter />);
  //
  //   expect(wrapper).toMatchSnapshot();
  // });

  // it('operates filter callback function correctly', () => {
  //   const filterAction = jest.fn();
  //   const wrapper = shallow(<RoomFilter filter={filterAction} />);
  //
  //   wrapper.find('#roomlist-all').simulate('click', {}, 'food');
  //
  //   expect(filterAction).toHaveBeenCalledWith('foo');
  // });
});
